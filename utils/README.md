# Utils

`utils/` is for small importable helpers used by the active runtime.

Current active helper:

- `global_seed.py`: shared deterministic seeding for training, evaluation, and
  Phase 3 scripts.

Keep command-line workflows in `scripts/`. Keep old one-off maintenance scripts
in `legacy/scripts/`.
